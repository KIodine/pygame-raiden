class CirclePath():
    
    def __init__(self,
            nowx,
            nowy,
            self.finalX,
            self.finalY,
            radius,
            self.clockwise = 0#0為順時針,1為逆時針,若未給值預設為0
        ):
        
        self.enabled = True
        self.omega = None
        self.s = 
    
    def NextCoor(nowx, nowy):
        if self.enabled = True:
            
            